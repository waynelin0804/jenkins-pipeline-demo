pipeline {
  agent none
  stages {
    stage('Hello') {
      agent any
      steps {
        sh 'echo hello!!!'
        sh 'echo webhook test dev!!!'
        sh 'echo "from dev!"'
      }
    }
  }
}
